# Default build (uses release profile)
build:
	cargo build --release

# Build for development (debug profile)
dev:
	cargo build

# Run the application
run:
	cargo run

# Clean build artifacts
clean:
	cargo clean

# Optional: Install dependencies
install:
	cargo fetch

.PHONY: build dev run clean install
